[project]
name = "rl-hyperheuristic-tsp"
version = "0.1.0"
description = "DRL-guided adaptive local search for the Traveling Salesman Problem"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["src*"]